{
  "name": "beanrelief",
  "version": "1.0.0",
  "description": "A mutual aid fund for friends of bean",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "concurrently 'cross-env NODE_ENV=development nodemon index.js' 'cd client && npm run start'",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mandrill-api": "^1.0.45"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "cross-env": "^7.0.2",
    "nodemon": "^2.0.4"
  }
}
